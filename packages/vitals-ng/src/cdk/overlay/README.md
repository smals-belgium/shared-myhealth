# @myhealth/vitals-ng/cdk/overlay

Secondary entry point of `@myhealth/vitals-ng` used for building Angular services that create overlay component,
such as dialogs, popovers, snackbars, toolips and the like.
It can be used by importing from `@myhealth/vitals-ng/cdk/overlay`.
