{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "tags": ["type:dk"],
  "targets": {
    "generate-manifest": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/custom-elements.json"],
      "options": {
        "command": "cem analyze",
        "cwd": "packages/design-kit"
      }
    }
  }
}
