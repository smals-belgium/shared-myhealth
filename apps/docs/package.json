{
  "name": "@smals-belgium-shared/docs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "eleventy --config=eleventy.config.mjs",
    "serve": "eleventy --serve --watch --config=eleventy.config.mjs",
    "clean": "rm -rf dist"
  },
  "nx": {
    "name": "docs",
    "tags": [
      "apps",
      "docs"
    ]
  }
}
